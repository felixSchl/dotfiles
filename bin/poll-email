#!/bin/bash
set -eo pipefail

account=$1

if [[ -z "$account" ]]; then
	echo >&2 "usage: report-new-mail <account>"
	exit 1
fi

if ! [[ -d ~/mail/"$account" ]]; then
	echo >&2 "Unknown account: $account"
	exit 1
fi

mbsync -L "$account"-inbox
report-new-mail "$account"
